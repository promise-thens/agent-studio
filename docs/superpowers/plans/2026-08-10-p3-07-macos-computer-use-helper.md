# P3-07 macOS Computer Use Helper 实施计划

> **致执行者：** 优先按任务顺序逐项落地，并在每个任务完成后做业务逻辑验证。步骤使用复选框 (`- [ ]`) 语法进行跟踪。

**优先级：** P3 / 权重 3（高权限原生能力）

**目标：** 以独立 macOS Helper 提供 Accessibility、ScreenCaptureKit 与输入控制，并用醒目停止入口和逐项高风险审批保护用户。

**核心数据流：** Capability 请求窗口/屏幕读取或输入操作；Broker 检查系统权限、目标应用、操作风险和授权；Helper 执行最小动作并返回摘要事件。

**约束与边界：** 不绕过 macOS 辅助功能/屏幕录制授权；不默认全屏连续录制、不读剪贴板全文、不后台常驻；只在用户可见状态运行。

**主要风险：** 屏幕与输入操作不可预测且难撤销；首期动作白名单、目标应用确认、节流、紧急停止、前台可见提示与失败即停。

**技术栈：** Electron 39、Vue 3、TypeScript、electron-vite、pnpm 10、Node.js 20+、Vitest；需要 Runtime 协议时以当期官方 schema 与本机实测为准。

---

## 实施范围

**前置依赖：**
- 依赖 P3-02；仅在产品确认首期支持 macOS 后实施。

**文件范围：**
- 新增 `apps/macos-helper/` 或受控原生模块、`src/main/capability/computer-use/`、IPC 协议和测试；更新 Manifest。

**安全策略：**
- 安全是与操作风险匹配的护栏：低风险只读操作可在任务范围授权；写入与命令需展示目标和影响；删除、外发数据、登录态、屏幕和剪贴板始终显式确认。
- Renderer 不接触密钥、任意 IPC、文件系统或子进程；所有跨进程数据须可序列化、限长并脱敏。

### 任务 1: 确认系统权限与范围

**任务目标：**
- 分别检测 Accessibility、Screen Recording，未授权时引导系统设置但不伪造已授权。

**涉及范围：**
- 新增 `apps/macos-helper/` 或受控原生模块、`src/main/capability/computer-use/`、IPC 协议和测试；更新 Manifest。

**前置依赖：**
- 依赖 P3-02；仅在产品确认首期支持 macOS 后实施。

- [ ] **第 1 步: 落地本任务**
说明：分别检测 Accessibility、Screen Recording，未授权时引导系统设置但不伪造已授权。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 不绕过 macOS 辅助功能/屏幕录制授权；不默认全屏连续录制、不读剪贴板全文、不后台常驻；只在用户可见状态运行。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 2: 实现受控动作协议

**任务目标：**
- 按读取应用状态、截图、点击、输入、滚动、拖拽、快捷键定义明确目标和前置截图/坐标校验。

**涉及范围：**
- 新增 `apps/macos-helper/` 或受控原生模块、`src/main/capability/computer-use/`、IPC 协议和测试；更新 Manifest。

**前置依赖：**
- 依赖本计划任务 1 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：按读取应用状态、截图、点击、输入、滚动、拖拽、快捷键定义明确目标和前置截图/坐标校验。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 不绕过 macOS 辅助功能/屏幕录制授权；不默认全屏连续录制、不读剪贴板全文、不后台常驻；只在用户可见状态运行。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 3: 验证停止与异常

**任务目标：**
- 覆盖权限撤销、目标窗口变化、Helper 崩溃、用户停止、长动作和敏感应用场景。

**涉及范围：**
- 新增 `apps/macos-helper/` 或受控原生模块、`src/main/capability/computer-use/`、IPC 协议和测试；更新 Manifest。

**前置依赖：**
- 依赖本计划任务 2 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：覆盖权限撤销、目标窗口变化、Helper 崩溃、用户停止、长动作和敏感应用场景。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 不绕过 macOS 辅助功能/屏幕录制授权；不默认全屏连续录制、不读剪贴板全文、不后台常驻；只在用户可见状态运行。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

## 验收标准

- [ ] 未授予系统权限无法执行；用户可随时停止并看到当前控制状态；每个输入/屏幕行为有可理解摘要和审批记录。
- [ ] 相关新增核心函数、IPC Handler、密钥/权限边界均有中文注释，且新增测试只使用假凭据和本地 Mock。
- [ ] 目标文件 ESLint、相关 Vitest、`pnpm typecheck`、`pnpm build` 与 `git diff --check` 在 Node.js 20+、pnpm 10.x 下完成；UI/Electron 改动另有对应开发版手工走查记录。
