# P4-02 双 Runtime 对比 实施计划

> **致执行者：** 优先按任务顺序逐项落地，并在每个任务完成后做业务逻辑验证。步骤使用复选框 (`- [ ]`) 语法进行跟踪。

**优先级：** P4 / 权重 3（差异化体验）

**目标：** 在相同任务基线下让 Grok 与 Codex 在隔离 worktree 执行，并以 Diff、测试、成本、耗时和用户选择进行可解释比较。

**核心数据流：** 用户确定任务、基准与预算；系统创建两个 task/worktree 快照；分别执行并汇总统一事件；比较页按同一维度展示结果而不评判“绝对更好”。

**约束与边界：** 不默认双跑花费；每个 Runtime 独立权限和预算；不自动合并任何一侧结果，不把不同模型/工具能力当成公平性能结论。

**主要风险：** 对比基线不同、成本失控或两边修改同一远程资源；限定本地隔离、显示差异条件、预算上限和外部副作用逐次审批。

**技术栈：** Electron 39、Vue 3、TypeScript、electron-vite、pnpm 10、Node.js 20+、Vitest；需要 Runtime 协议时以当期官方 schema 与本机实测为准。

---

## 实施范围

**前置依赖：**
- 依赖 P2-04、P4-01、P0-07。

**文件范围：**
- 新增 `src/main/agent/comparison-service.ts`、测试；新增 `ComparisonSetup.vue`、`ComparisonReview.vue`。

**安全策略：**
- 安全是与操作风险匹配的护栏：低风险只读操作可在任务范围授权；写入与命令需展示目标和影响；删除、外发数据、登录态、屏幕和剪贴板始终显式确认。
- Renderer 不接触密钥、任意 IPC、文件系统或子进程；所有跨进程数据须可序列化、限长并脱敏。

### 任务 1: 定义比较输入与公平说明

**任务目标：**
- 固定 prompt、项目基准、Runtime/Profile/模型版本、Capability 和预算，并显示不可比较条件。

**涉及范围：**
- 新增 `src/main/agent/comparison-service.ts`、测试；新增 `ComparisonSetup.vue`、`ComparisonReview.vue`。

**前置依赖：**
- 依赖 P2-04、P4-01、P0-07。

- [ ] **第 1 步: 落地本任务**
说明：固定 prompt、项目基准、Runtime/Profile/模型版本、Capability 和预算，并显示不可比较条件。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 不默认双跑花费；每个 Runtime 独立权限和预算；不自动合并任何一侧结果，不把不同模型/工具能力当成公平性能结论。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 2: 编排双任务

**任务目标：**
- 创建独立 worktree、独立 taskId、独立授权，处理一侧失败/取消时保留另一侧结果。

**涉及范围：**
- 新增 `src/main/agent/comparison-service.ts`、测试；新增 `ComparisonSetup.vue`、`ComparisonReview.vue`。

**前置依赖：**
- 依赖本计划任务 1 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：创建独立 worktree、独立 taskId、独立授权，处理一侧失败/取消时保留另一侧结果。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 不默认双跑花费；每个 Runtime 独立权限和预算；不自动合并任何一侧结果，不把不同模型/工具能力当成公平性能结论。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 3: 实现结果审阅

**任务目标：**
- 按方案、Diff、测试、时间、Usage、成本和风险摘要展示，用户显式选择保留、继续或丢弃。

**涉及范围：**
- 新增 `src/main/agent/comparison-service.ts`、测试；新增 `ComparisonSetup.vue`、`ComparisonReview.vue`。

**前置依赖：**
- 依赖本计划任务 2 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：按方案、Diff、测试、时间、Usage、成本和风险摘要展示，用户显式选择保留、继续或丢弃。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 不默认双跑花费；每个 Runtime 独立权限和预算；不自动合并任何一侧结果，不把不同模型/工具能力当成公平性能结论。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

## 验收标准

- [ ] 两侧改动物理隔离；比较条件和数据来源透明；用户未确认前没有代码被合并或外部副作用被重复执行。
- [ ] 相关新增核心函数、IPC Handler、密钥/权限边界均有中文注释，且新增测试只使用假凭据和本地 Mock。
- [ ] 目标文件 ESLint、相关 Vitest、`pnpm typecheck`、`pnpm build` 与 `git diff --check` 在 Node.js 20+、pnpm 10.x 下完成；UI/Electron 改动另有对应开发版手工走查记录。
