# P0-08 统一权限 Broker 实施计划

> **致执行者：** 优先按任务顺序逐项落地，并在每个任务完成后做业务逻辑验证。步骤使用复选框 (`- [ ]`) 语法进行跟踪。

**优先级：** P0 / 权重 5（跨 Runtime 与能力包的必要护栏）

**目标：** 用一次统一的风险分级、授权范围和审批记录处理 Runtime 与未来 Capability Pack 的外部副作用请求。

**核心数据流：** Adapter/能力包提交标准操作意图；Broker 根据风险、目标和已有授权判定允许/请求审批/拒绝；结果经事件回传发起者并记录脱敏审计。

**约束与边界：** 不为读项目元信息等低风险动作逐次弹窗；写文件、执行命令显示影响；删除、外发数据、登录态、屏幕和剪贴板始终明确确认。

**主要风险：** “本次任务允许”被错误扩张到其它项目；授权键必须绑定 task/session、项目 canonical path、操作类型和参数约束并设置有效期。

**技术栈：** Electron 39、Vue 3、TypeScript、electron-vite、pnpm 10、Node.js 20+、Vitest；需要 Runtime 协议时以当期官方 schema 与本机实测为准。

---

## 实施范围

**前置依赖：**
- 依赖 P0-01、P0-02、P0-04。

**文件范围：**
- 新增 `src/main/security/permission-broker.ts`、`permission-policy.ts`、测试；`src/shared/agent.ts`；`src/renderer/src/components/PermissionPrompt.vue`。

**安全策略：**
- 安全是与操作风险匹配的护栏：低风险只读操作可在任务范围授权；写入与命令需展示目标和影响；删除、外发数据、登录态、屏幕和剪贴板始终显式确认。
- Renderer 不接触密钥、任意 IPC、文件系统或子进程；所有跨进程数据须可序列化、限长并脱敏。

### 任务 1: 定义操作与风险模型

**任务目标：**
- 覆盖读取、写入、命令、网络外发、删除、浏览器登录态、屏幕/剪贴板；明确不可长期授权的高风险类别。

**涉及范围：**
- 新增 `src/main/security/permission-broker.ts`、`permission-policy.ts`、测试；`src/shared/agent.ts`；`src/renderer/src/components/PermissionPrompt.vue`。

**前置依赖：**
- 依赖 P0-01、P0-02、P0-04。

- [ ] **第 1 步: 落地本任务**
说明：覆盖读取、写入、命令、网络外发、删除、浏览器登录态、屏幕/剪贴板；明确不可长期授权的高风险类别。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 不为读项目元信息等低风险动作逐次弹窗；写文件、执行命令显示影响；删除、外发数据、登录态、屏幕和剪贴板始终明确确认。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 2: 实现授权匹配与审计

**任务目标：**
- 先检查精确范围授权，再创建带发起者、目标、影响和有效期的审批；审计仅存摘要与结果。

**涉及范围：**
- 新增 `src/main/security/permission-broker.ts`、`permission-policy.ts`、测试；`src/shared/agent.ts`；`src/renderer/src/components/PermissionPrompt.vue`。

**前置依赖：**
- 依赖本计划任务 1 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：先检查精确范围授权，再创建带发起者、目标、影响和有效期的审批；审计仅存摘要与结果。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 不为读项目元信息等低风险动作逐次弹窗；写文件、执行命令显示影响；删除、外发数据、登录态、屏幕和剪贴板始终明确确认。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 3: 接入 Grok 权限与 UI

**任务目标：**
- 将 ACP 权限请求转换为 Broker 请求，验证拒绝、超时、取消、相同任务范围授权和项目切换全部安全收口。

**涉及范围：**
- 新增 `src/main/security/permission-broker.ts`、`permission-policy.ts`、测试；`src/shared/agent.ts`；`src/renderer/src/components/PermissionPrompt.vue`。

**前置依赖：**
- 依赖本计划任务 2 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：将 ACP 权限请求转换为 Broker 请求，验证拒绝、超时、取消、相同任务范围授权和项目切换全部安全收口。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 不为读项目元信息等低风险动作逐次弹窗；写文件、执行命令显示影响；删除、外发数据、登录态、屏幕和剪贴板始终明确确认。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

## 验收标准

- [ ] 任何 Runtime 或插件无法绕过 Broker；审批说明具体到目标和影响；低风险流程不卡顿，高风险操作不可静默通过。
- [ ] 相关新增核心函数、IPC Handler、密钥/权限边界均有中文注释，且新增测试只使用假凭据和本地 Mock。
- [ ] 目标文件 ESLint、相关 Vitest、`pnpm typecheck`、`pnpm build` 与 `git diff --check` 在 Node.js 20+、pnpm 10.x 下完成；UI/Electron 改动另有对应开发版手工走查记录。
