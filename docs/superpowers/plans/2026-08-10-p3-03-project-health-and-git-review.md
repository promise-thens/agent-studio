# P3-03 项目体检与 Git 审阅 实施计划

> **致执行者：** 优先按任务顺序逐项落地，并在每个任务完成后做业务逻辑验证。步骤使用复选框 (`- [ ]`) 语法进行跟踪。

**优先级：** P3 / 权重 4（开发任务的可解释起点）

**目标：** 提供项目识别、环境体检、Git 状态、Diff、检查点与受控回滚建议，让任务开始和完成都有可观察依据。

**核心数据流：** 用户选定项目后，受控能力读取允许的元数据；结果摘要进入 task context 与时间线；写检查点、恢复等动作经 Broker 和 Git 命令白名单执行。

**约束与边界：** 默认只读；不自动 git add/commit/push/reset/stash；检查点使用明确可恢复分支或 patch，并事先说明范围。

**主要风险：** 把脏工作区当作 Agent 修改或在错误目录执行 Git；每次操作解析 canonical project root、记录基线并显示用户已有改动。

**技术栈：** Electron 39、Vue 3、TypeScript、electron-vite、pnpm 10、Node.js 20+、Vitest；需要 Runtime 协议时以当期官方 schema 与本机实测为准。

---

## 实施范围

**前置依赖：**
- 依赖 P0-07、P0-08、P3-01。

**文件范围：**
- 新增 `src/main/capability/project-health.ts`、`git-review.ts`、测试；新增 Renderer 项目概览与 Diff 组件。

**安全策略：**
- 安全是与操作风险匹配的护栏：低风险只读操作可在任务范围授权；写入与命令需展示目标和影响；删除、外发数据、登录态、屏幕和剪贴板始终显式确认。
- Renderer 不接触密钥、任意 IPC、文件系统或子进程；所有跨进程数据须可序列化、限长并脱敏。

### 任务 1: 实现只读项目体检

**任务目标：**
- 识别 Git 根、Node/pnpm、锁文件、工作区状态和常用脚本，限制扫描深度与输出大小。

**涉及范围：**
- 新增 `src/main/capability/project-health.ts`、`git-review.ts`、测试；新增 Renderer 项目概览与 Diff 组件。

**前置依赖：**
- 依赖 P0-07、P0-08、P3-01。

- [ ] **第 1 步: 落地本任务**
说明：识别 Git 根、Node/pnpm、锁文件、工作区状态和常用脚本，限制扫描深度与输出大小。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 默认只读；不自动 git add/commit/push/reset/stash；检查点使用明确可恢复分支或 patch，并事先说明范围。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 2: 实现 Diff 与检查点能力

**任务目标：**
- 区分基线前已有修改与任务新增修改；对创建检查点和恢复请求使用具体目标与明确审批。

**涉及范围：**
- 新增 `src/main/capability/project-health.ts`、`git-review.ts`、测试；新增 Renderer 项目概览与 Diff 组件。

**前置依赖：**
- 依赖本计划任务 1 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：区分基线前已有修改与任务新增修改；对创建检查点和恢复请求使用具体目标与明确审批。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 默认只读；不自动 git add/commit/push/reset/stash；检查点使用明确可恢复分支或 patch，并事先说明范围。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 3: 验证安全路径

**任务目标：**
- 在干净、脏、非 Git、嵌套仓库和 Git 命令失败场景下走查，确保从不自动覆盖用户文件。

**涉及范围：**
- 新增 `src/main/capability/project-health.ts`、`git-review.ts`、测试；新增 Renderer 项目概览与 Diff 组件。

**前置依赖：**
- 依赖本计划任务 2 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：在干净、脏、非 Git、嵌套仓库和 Git 命令失败场景下走查，确保从不自动覆盖用户文件。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 默认只读；不自动 git add/commit/push/reset/stash；检查点使用明确可恢复分支或 patch，并事先说明范围。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

## 验收标准

- [ ] 用户能区分已有脏改动和本任务修改；所有破坏性 Git 行为需要具体确认；体检失败不会阻止无关只读查看。
- [ ] 相关新增核心函数、IPC Handler、密钥/权限边界均有中文注释，且新增测试只使用假凭据和本地 Mock。
- [ ] 目标文件 ESLint、相关 Vitest、`pnpm typecheck`、`pnpm build` 与 `git diff --check` 在 Node.js 20+、pnpm 10.x 下完成；UI/Electron 改动另有对应开发版手工走查记录。
