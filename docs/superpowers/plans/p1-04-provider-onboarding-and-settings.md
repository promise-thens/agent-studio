# P1-04 Provider 首次引导与设置体验 实施计划

> **致执行者：** 优先按任务顺序逐项落地，并在每个任务完成后做业务逻辑验证。步骤使用复选框 (`- [ ]`) 语法进行跟踪。

**优先级：** P1 / 权重 4（已有实现，逐步演进）

**目标：** 让无可用配置的用户完成可理解的首次配置，并在设置中安全查看、修改、测试和清除 Provider 摘要。

**核心数据流：** App 读取主进程 Provider 摘要；未配置时呈现 Onboarding；保存请求经过测试、持久化和 Runtime 绑定事务；成功后 UI 只更新摘要。

**约束与边界：** 没有配置时不展示会失败的工作台连接流程；保存后只显示“已保存”；不乐观显示模型/Runtime 已切换。

**主要风险：** 保存、测试、运行中切换并发造成 UI 与主进程不同步；按钮状态以主进程返回为准，执行任务期间锁定模型切换。

**技术栈：** Electron 39、Vue 3、TypeScript、electron-vite、pnpm 10、Node.js 20+、Vitest；需要 Runtime 协议时以当期官方 schema 与本机实测为准。

---

## 实施范围

**前置依赖：**
- 依赖 P1-01 至 P1-03。

**文件范围：**
- 复核 `src/renderer/src/components/ProviderOnboarding.vue`、`ModelSelector.vue`、`App.vue`、Provider IPC 类型与样式。

**安全策略：**
- 安全是与操作风险匹配的护栏：低风险只读操作可在任务范围授权；写入与命令需展示目标和影响；删除、外发数据、登录态、屏幕和剪贴板始终显式确认。
- Renderer 不接触密钥、任意 IPC、文件系统或子进程；所有跨进程数据须可序列化、限长并脱敏。

### 任务 1: 梳理状态机

**任务目标：**
- 列出空态、编辑、模型加载、测试中、保存中、失败、成功、清除和恢复，给每种状态具体字段反馈。

**涉及范围：**
- 复核 `src/renderer/src/components/ProviderOnboarding.vue`、`ModelSelector.vue`、`App.vue`、Provider IPC 类型与样式。

**前置依赖：**
- 依赖 P1-01 至 P1-03。

- [ ] **第 1 步: 落地本任务**
说明：列出空态、编辑、模型加载、测试中、保存中、失败、成功、清除和恢复，给每种状态具体字段反馈。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 没有配置时不展示会失败的工作台连接流程；保存后只显示“已保存”；不乐观显示模型/Runtime 已切换。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 2: 完成多 Profile 兼容 UI

**任务目标：**
- 配置页管理 Profile，输入框 footer 仅放快捷模型选择；模型长名省略并可查看完整标题。

**涉及范围：**
- 复核 `src/renderer/src/components/ProviderOnboarding.vue`、`ModelSelector.vue`、`App.vue`、Provider IPC 类型与样式。

**前置依赖：**
- 依赖本计划任务 1 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：配置页管理 Profile，输入框 footer 仅放快捷模型选择；模型长名省略并可查看完整标题。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 没有配置时不展示会失败的工作台连接流程；保存后只显示“已保存”；不乐观显示模型/Runtime 已切换。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 3: 手工可用性回归

**任务目标：**
- 在小窗口和键盘操作下验证焦点、错误关联、重试、HTTP 警示、清除和运行中锁定。

**涉及范围：**
- 复核 `src/renderer/src/components/ProviderOnboarding.vue`、`ModelSelector.vue`、`App.vue`、Provider IPC 类型与样式。

**前置依赖：**
- 依赖本计划任务 2 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：在小窗口和键盘操作下验证焦点、错误关联、重试、HTTP 警示、清除和运行中锁定。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 没有配置时不展示会失败的工作台连接流程；保存后只显示“已保存”；不乐观显示模型/Runtime 已切换。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

## 验收标准

- [ ] 首次使用不会进入不可操作工作台；失败不丢失用户填写内容；密钥从不回填，模型/Runtime 状态以主进程确认值为准。
- [ ] 相关新增核心函数、IPC Handler、密钥/权限边界均有中文注释，且新增测试只使用假凭据和本地 Mock。
- [ ] 目标文件 ESLint、相关 Vitest、`pnpm typecheck`、`pnpm build` 与 `git diff --check` 在 Node.js 20+、pnpm 10.x 下完成；UI/Electron 改动另有对应开发版手工走查记录。
