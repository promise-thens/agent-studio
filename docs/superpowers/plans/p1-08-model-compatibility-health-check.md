# P1-08 模型兼容性体检 实施计划

> **致执行者：** 优先按任务顺序逐项落地，并在每个任务完成后做业务逻辑验证。步骤使用复选框 (`- [ ]`) 语法进行跟踪。

**优先级：** P1 / 权重 4（避免连通性误导）

**目标：** 以八级、可复现的体检向用户展示模型和 Runtime 组合的实际 Agent 能力、限制与成本提示。

**核心数据流：** 用户选择 Profile、模型和 Runtime；主进程按协议与 Runtime 运行允许的探针；结果写入带版本和时间的健康摘要，Renderer 展示分级结论。

**约束与边界：** 探针默认低成本、可取消并先请求用户确认可能计费的层级；不自动执行破坏性工具、不以单次成功承诺长期可靠性。

**主要风险：** 真实服务费用和限流、探针误伤用户配置；分级执行、明确预计调用次数、缓存结果、允许跳过可选层。

**技术栈：** Electron 39、Vue 3、TypeScript、electron-vite、pnpm 10、Node.js 20+、Vitest；需要 Runtime 协议时以当期官方 schema 与本机实测为准。

---

## 实施范围

**前置依赖：**
- 依赖 P1-03、P1-07 和 Runtime 能力矩阵。

**文件范围：**
- 新增 `src/main/provider/compatibility-checker.ts`、测试；`src/shared/provider-health.ts`；`src/renderer/src/components/ModelHealthReport.vue`。

**安全策略：**
- 安全是与操作风险匹配的护栏：低风险只读操作可在任务范围授权；写入与命令需展示目标和影响；删除、外发数据、登录态、屏幕和剪贴板始终显式确认。
- Renderer 不接触密钥、任意 IPC、文件系统或子进程；所有跨进程数据须可序列化、限长并脱敏。

### 任务 1: 定义八级结果

**任务目标：**
- 覆盖 URL/认证、基础请求、流式、工具调用、工具回传、多轮完成、中断错误、图片/Reasoning 可选能力。

**涉及范围：**
- 新增 `src/main/provider/compatibility-checker.ts`、测试；`src/shared/provider-health.ts`；`src/renderer/src/components/ModelHealthReport.vue`。

**前置依赖：**
- 依赖 P1-03、P1-07 和 Runtime 能力矩阵。

- [ ] **第 1 步: 落地本任务**
说明：覆盖 URL/认证、基础请求、流式、工具调用、工具回传、多轮完成、中断错误、图片/Reasoning 可选能力。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 探针默认低成本、可取消并先请求用户确认可能计费的层级；不自动执行破坏性工具、不以单次成功承诺长期可靠性。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 2: 实现无副作用探针

**任务目标：**
- 使用最小虚拟工具和可控超时，记录协议/Runtime 版本、时间、阶段、延迟和脱敏错误。

**涉及范围：**
- 新增 `src/main/provider/compatibility-checker.ts`、测试；`src/shared/provider-health.ts`；`src/renderer/src/components/ModelHealthReport.vue`。

**前置依赖：**
- 依赖本计划任务 1 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：使用最小虚拟工具和可控超时，记录协议/Runtime 版本、时间、阶段、延迟和脱敏错误。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 探针默认低成本、可取消并先请求用户确认可能计费的层级；不自动执行破坏性工具、不以单次成功承诺长期可靠性。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 3: 呈现与回归

**任务目标：**
- 显示完整 Agent、基础聊天、缺工具、协议不兼容、未验证；验证取消、429、收费提示与旧结果过期。

**涉及范围：**
- 新增 `src/main/provider/compatibility-checker.ts`、测试；`src/shared/provider-health.ts`；`src/renderer/src/components/ModelHealthReport.vue`。

**前置依赖：**
- 依赖本计划任务 2 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：显示完整 Agent、基础聊天、缺工具、协议不兼容、未验证；验证取消、429、收费提示与旧结果过期。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 探针默认低成本、可取消并先请求用户确认可能计费的层级；不自动执行破坏性工具、不以单次成功承诺长期可靠性。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

## 验收标准

- [ ] 连接成功不再被显示为完整 Agent 兼容；每个失败有阶段与下一步；体检不自动产生文件、命令或外发副作用。
- [ ] 相关新增核心函数、IPC Handler、密钥/权限边界均有中文注释，且新增测试只使用假凭据和本地 Mock。
- [ ] 目标文件 ESLint、相关 Vitest、`pnpm typecheck`、`pnpm build` 与 `git diff --check` 在 Node.js 20+、pnpm 10.x 下完成；UI/Electron 改动另有对应开发版手工走查记录。
