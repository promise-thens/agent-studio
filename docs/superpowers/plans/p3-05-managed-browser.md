# P3-05 应用内受管浏览器 实施计划

> **致执行者：** 优先按任务顺序逐项落地，并在每个任务完成后做业务逻辑验证。步骤使用复选框 (`- [ ]`) 语法进行跟踪。

**优先级：** P3 / 权重 3（浏览器能力第一阶段）

> **2026-08-31：** 本计划后置于 [P0-19f](p0-19f-browser-computer-use-surface.md)。Grok 插件浏览器表面未打磨、且产品未确认「仍缺共享页面」前不开工。
>
> **2026-09-04：已被 [P0-21](p0-21-host-managed-browser.md) 取代，不开工。** 产品已确认 Codex 式宿主内置浏览器（独立 Profile、WebContentsView、Grok 经窄动作 MCP 操作同一页）。本文件保留作历史计划，执行者不要按本文件建 `capability/browser` 或等 P3-01。Chrome 扩展桥仍见 [P3-06](p3-06-chrome-native-bridge.md)。

**目标：** 提供独立 Profile 的受管浏览器供 Agent 执行网页任务，不读取用户 Chrome Profile 或绕过登录态控制。

**核心数据流：** Capability 创建独立浏览器上下文；Runtime 请求导航/读取/交互意图；Broker 根据网站、数据敏感性和副作用授权，事件在时间线记录摘要。

**约束与边界：** 使用独立 Profile；不读取现有 Chrome cookie/history；涉及登录、下载、上传、外发表单与权限提示必须单独说明。

**主要风险：** 网页内容和登录态敏感且站点行为不可预测；域名范围授权、会话隔离、可见停止、下载目录限制与审计摘要。

**技术栈：** Electron 39、Vue 3、TypeScript、electron-vite、pnpm 10、Node.js 20+、Vitest；需要 Runtime 协议时以当期官方 schema 与本机实测为准。

---

## 实施范围

**前置依赖：**
- 依赖 P3-01、P3-02、P3-04。

**文件范围：**
- 新增 `src/main/capability/browser/managed-browser.ts`、测试；新增浏览器面板和 Capability Manifest。

**安全策略：**
- 安全是与操作风险匹配的护栏：低风险只读操作可在任务范围授权；写入与命令需展示目标和影响；删除、外发数据、登录态、屏幕和剪贴板始终显式确认。
- Renderer 不接触密钥、任意 IPC、文件系统或子进程；所有跨进程数据须可序列化、限长并脱敏。

### 任务 1: 确定技术与隔离模型

**任务目标：**
- 在 Playwright/CDP 实现中选定受支持方案，定义 Profile 创建、清理和崩溃恢复而不触碰用户数据。

**涉及范围：**
- 新增 `src/main/capability/browser/managed-browser.ts`、测试；新增浏览器面板和 Capability Manifest。

**前置依赖：**
- 依赖 P3-01、P3-02、P3-04。

- [ ] **第 1 步: 落地本任务**
说明：在 Playwright/CDP 实现中选定受支持方案，定义 Profile 创建、清理和崩溃恢复而不触碰用户数据。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 使用独立 Profile；不读取现有 Chrome cookie/history；涉及登录、下载、上传、外发表单与权限提示必须单独说明。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 2: 实现最小网页动作

**任务目标：**
- 先支持导航、可见文本读取、截图摘要和受控点击；所有动作经 Capability/Broker，不暴露任意 CDP。

**涉及范围：**
- 新增 `src/main/capability/browser/managed-browser.ts`、测试；新增浏览器面板和 Capability Manifest。

**前置依赖：**
- 依赖本计划任务 1 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：先支持导航、可见文本读取、截图摘要和受控点击；所有动作经 Capability/Broker，不暴露任意 CDP。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 使用独立 Profile；不读取现有 Chrome cookie/history；涉及登录、下载、上传、外发表单与权限提示必须单独说明。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

### 任务 3: 验证敏感路径

**任务目标：**
- 覆盖域名越界、登录页、文件下载、上传、弹窗、停止和 Profile 清理，确认审计无 cookie/页面全文。

**涉及范围：**
- 新增 `src/main/capability/browser/managed-browser.ts`、测试；新增浏览器面板和 Capability Manifest。

**前置依赖：**
- 依赖本计划任务 2 的可验证输出。

- [ ] **第 1 步: 落地本任务**
说明：覆盖域名越界、登录页、文件下载、上传、弹窗、停止和 Profile 清理，确认审计无 cookie/页面全文。

- [ ] **第 2 步: 业务逻辑验证**
说明：使用单元测试、受控 mock 或开发版手工路径验证主流程。
预期：输入、状态与输出符合本计划的数据流，不出现未声明的副作用。

- [ ] **第 3 步: 边界与风险检查**
说明：检查 使用独立 Profile；不读取现有 Chrome cookie/history；涉及登录、下载、上传、外发表单与权限提示必须单独说明。
预期：失败路径有明确、脱敏的反馈，既不越权也不阻断正常低风险流程。

## 验收标准

- [ ] 浏览器会话与用户 Chrome 隔离；高敏感网页动作需要明确授权；用户始终能看到并停止受管浏览器。
- [ ] 相关新增核心函数、IPC Handler、密钥/权限边界均有中文注释，且新增测试只使用假凭据和本地 Mock。
- [ ] 目标文件 ESLint、相关 Vitest、`pnpm typecheck`、`pnpm build` 与 `git diff --check` 在 Node.js 20+、pnpm 10.x 下完成；UI/Electron 改动另有对应开发版手工走查记录。
